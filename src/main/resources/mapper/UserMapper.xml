<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.lll44556.gulimall.auth.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="top.lll44556.lll.auth.entity.User">
            <id property="id" column="id" jdbcType="BIGINT"/>
            <result property="username" column="username" jdbcType="VARCHAR"/>
            <result property="password" column="password" jdbcType="VARCHAR"/>
            <result property="orgId" column="org_id" jdbcType="INTEGER"/>
            <result property="enabled" column="enabled" jdbcType="TINYINT"/>
            <result property="phone" column="phone" jdbcType="VARCHAR"/>
            <result property="email" column="email" jdbcType="VARCHAR"/>
            <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,username,password,
        org_id,enabled,phone,
        email,create_time
    </sql>

    <select id="selectRules" resultType="string">
        select DISTINCT m.url
        from `user` u
                 left join user_role ur
                           on u.id=ur.user_id
                 left join role_menu rm
                           on ur.role_id=rm.role_id
                 left join menu m
                           on rm.menu_id=m.id
        where u.username=#{username}
    </select>
</mapper>
